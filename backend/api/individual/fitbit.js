module.exports = (env, router) => {

  // router.get('/auth/fitbit', passport.authenticate('fitbit', { scope: ['activity','heartrate','location','profile'] }));
  //
  // router.get( '/auth/fitbit/callback', passport.authenticate( 'fitbit', { successRedirect: '/auth/fitbit/success', failureRedirect: '/auth/fitbit/failure'}));
  //
  // router.get('/auth/fitbit/success', function(req, res){ res.render('success.ejs', {}) });
  //
  // router.get('/auth/fitbit/failure', function(req, res){ res.render('fail.ejs', {}) });
}
