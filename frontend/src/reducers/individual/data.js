import {} from "constants/individual/data";

const data = (state = {}, action) => {
    switch (action.type) {
        default:
            // VERY IMPORTANT
            return state;
    }
};


export default data;
