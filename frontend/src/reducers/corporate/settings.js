import {} from "constants/corporate/settings";

const settings = (state = {}, action) => {
    switch (action.type) {
        default:
            // VERY IMPORTANT
            return state;
    }
};


export default settings;
