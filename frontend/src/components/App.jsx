import React, { Component } from 'react';
import './App.scss';

export default class App extends Component {
    render() {
        return (
            <div className="basic">
                Hello not quite from react with or without HMRE! actually now it has HRME, actually it does. But maybe it does? Nope, oh wait it does - does it? Checkin 
            </div>
        );
    }
}

